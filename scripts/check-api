#!/usr/bin/env bash
# API edit loop: ruff + pytest for services/api. Blind to all Swift.
set -euo pipefail
cd "$(dirname "$0")/../services/api"
echo "── ruff ──"
uv run ruff check .
echo "── pytest ──"
uv run pytest -q
echo "✓ check-api green"
