# Frozen Decisions

Numbered, dated, with rationale. Don't re-litigate mid-phase — amend here + master plan Amendments log if something must change. Sub-plan-local decisions live in each sub-plan's "Decisions locked" block; this file holds the cross-phase ones.

## 2026-06-12 — Planning session (approved first pass)

1. **Single backend: FastAPI + Supabase (Beacon shape).** Serein's Go/Fly/Tigris side not carried; its enrichment-worker design re-implemented in Python. One stack for one developer.
2. **Foreground-only capture for P0.** No `VoiceCaptureIntent` / `VoiceLiveActivity` port. `UIBackgroundModes: audio` keeps in-progress recordings alive across app-switch/lock. Reversible post-beta.
3. **Claim ladder extended, never weakened:** `accepted → mic_active → confirmed_listening → saved` (Serein semantics verbatim) + derived `transcribed → parsed → logged`. "Saved" = audio durably committed locally, nothing more.
4. **Audio is ground truth; meal log is derived.** Corrections are append-only records referencing the parse — simultaneously training data and admin-audit trail. Never patch artifacts in place.
5. **Voice-only logging.** No text-search food entry (out of scope). Manual *editing* of parsed items pre-confirm is in scope. No add-item by text on the result screen.
6. **Design: Cal AI reference layout, black/gold palette.** `#FAF9F6` bg, `#F4F2EE` cards r24, `#1A1A1A`/`#8A8A8E` text, `#111111` pill CTAs, `#C4A35A` gold accent. Macro chips keep semantic colors (protein red / carbs amber / fats blue). SF Pro; 40–64pt semibold numerals. Light mode only in P0 (locked via `UIUserInterfaceStyle`).
7. **Auth: phone OTP via Supabase, ported from Beacon.** No social login ⇒ Sign in with Apple not required by App Review.
8. **Transcription: server-side ElevenLabs Scribe** from the FastAPI worker. Keeps audio auditable against the transcript; Serein-proven provider.
9. **Parser: Claude tool-forced structured output.** `PARSER_MODEL` env, default `claude-sonnet-4-6`; Haiku 4.5 evaluated in B7 (verdict pending — record here).
10. **LLM extracts, deterministic code calculates.** All macro math, conversions, thresholds, protocol targets, and check-in adjustments are tested Python. AI writes prose ("why", recommendations phrasing) from structured rule output it cannot override.
11. **Clarifying question: exactly one, threshold-gated.** Fires only when a missing detail could shift the meal >75 kcal or >10g of a macro; highest-impact candidate wins; skippable. Threshold defined once in `docs/PARSER_CONTRACT.md`.
12. **Dictionary-first nutrition resolution, USDA FDC second.** Curated internal dictionary (with raw/cooked + unit conversions, modifier math) answers high-frequency foods; FDC covers the long tail through a read-through cache.
13. **App group `group.com.vocal.shared` kept despite no P0 extensions.** Minimizes Serein port diffs; future-proofs share/widget surfaces.
14. **Offline-capable capture path** (inverts Beacon's "no offline mode" for capture only). Capture commits locally without signal; transcription/parse catch up. Server is authoritative for everything else.
15. **Result delivery by polling, not WebSockets** (~1.5s with backoff). Beacon convention; revisit only if the 30s log target is threatened by measured latency.
16. **No push notifications, no third-party analytics SDKs in P0.** Check-in nudges via concierge text message. Keeps the privacy form small and the binary clean.
17. **No HealthKit in P0.** Weight is self-reported in check-ins. Avoids entitlement + privacy-form scope before the beta gate justifies it.
18. **Protocol safety rails live in the engine, not the prompt:** deficit/surplus caps (~0.5–1% BW/week), calorie floors, protein bounds. Doubles as App Review health posture.
19. **Protocol versions are immutable rows** with `supersedes` FK; check-in acceptance creates v(n+1) via the engine. Tombstone deletes for meal logs.
20. **External-tester TestFlight track** ⇒ Beta App Review ⇒ privacy policy URL, account deletion, review notes are planned work (Phase I), not surprises.
21. **Admin access = Supabase auth + server-side email allowlist; all admin reads audit-logged.** Voice recordings and food diaries are sensitive; access must be explainable. Beta users are told about admin review in the privacy policy.
22. **The fixture corpus is binding** (doccure pattern): `scripts/parser-eval` SCORES regression does not merge.
23. **Never edit, delete, or restructure Beacon or Serein.** Copy out only; ported files carry a header naming source + deletions.

## 2026-06-18 — Right-sizing pass (keep it lean for a 5–10 user concierge beta)

Three simplifications adopted after a stack-complexity review. Each cuts a provider/dependency/build with no loss to the thesis; none waste work already done. Supersede the earlier decisions noted.

24. **Transcription is on-device (Apple, iOS 26 `SpeechTranscriber`/`SpeechAnalyzer`), not server-side ElevenLabs.** Supersedes #8. Removes a provider, an API key, and the server transcription hop; transcript is ready immediately (no upload-then-transcribe latency, helps the <30s gate) and audio never has to leave the device just to be read. Audio still uploads in the background as the immutable ground-truth/audit artifact. The parser is unaffected — it takes a transcript string regardless of origin. The C5 worker shrinks to "parse only" (the app posts the transcript to `/parse`); server-side re-transcription remains possible later for audit if on-device quality disappoints in the beta.
25. **Admin review (P0 #10) is a `scripts/review` CLI + Supabase Studio, not a Next.js web app.** Refines #21. One reviewer auditing ten users does not need a hosted web app with its own auth. `scripts/review <meal_id>` prints the signed audio URL + transcript + parse JSON + corrections; `scripts/beta-metrics` covers the gate numbers + confidence calibration. Supabase Studio gives table browsing free. Phase H collapses to these scripts. The admin allowlist/audit-log posture (#21) still applies to the signed-URL endpoint the CLI calls.
26. **Auth is Sign in with Apple, not phone OTP.** Supersedes #7. One tap on TestFlight, no SMS provider to integrate or pay for, and no phone-number PII — which shrinks the App Privacy form and Beta App Review surface. (The earlier worry that offering social login forces a Sign-in-with-Apple option is moot when SiwA *is* the login.)
27. **No Prometheus/Grafana stack stood up for the beta.** The observability code ported from Beacon stays dormant; the six beta-gate numbers come from `scripts/beta-metrics` (plain SQL). Operational call — costs nothing, removes a moving part.

## 2026-06-18 — Cofounder call (Lorenzo × Francesco). Source: `docs/PRODUCT_BRIEF.md`

Francesco is the nutrition cofounder; his coaching method is the protocol IP. These lock the product doctrine from the call. Two of them (28, 29) were the pending UI confirmations — Lorenzo confirmed "take your recommendation."

28. **Carbs & fat are OFF the home dashboard** (still computed, still on meal detail). Confirmed. Default dashboard = **calories left · protein · produce · fiber · water** — exactly Francesco's coaching pillars. No unsolicited macro nagging ("high in saturated fat" pop-ups are banned).
29. **Clarifying checks are per-material-ingredient, not one-per-meal.** Supersedes #11's cap. The >75 kcal / >10g threshold stays (won't ask about trivia); the cap lifts so EVERY ingredient whose unknown materially moves the meal gets its own check (burger → beef fat ratio + cheddar type + mayo type; "4oz 93/7 beef + rice" → zero). On-brand: effort by design.
30. **Micro-tracking is opt-in.** Fats/carbs/sodium/sugar are off by default; a user adds them via an edit screen (diabetic → sugar, hypertensive → sodium). New surface: a dashboard "edit metrics" screen.
31. **Three pillars are the whole product** ("doesn't need to be any more"): (1) voice-first logging, (2) personalized protocol from a deep human intake, (3) mid-week situational nudging. Anything outside these three waits.
32. **Mid-week situational nudging is promoted to a core pillar** — Francesco rates it *highest* ("the game changer, not voice first"). Proactive, *during* the week, context-aware ("haven't logged — rough day?"), backed by a bank of situational advice. This reframes Phase G from "weekly check-in form" to mid-week nudging (the Carbon-style weekly form is a later input, not the headline).
33. **Text logging is the MVP fallback** (search a food library, MyFitnessPal-style). Voice primary; text the safety net. Confirmed in-scope (the earlier "voice-only, no text-search" decision #5 is relaxed: text *search* is allowed as the fallback; the point was no friction, and text is the safety net, not the star).
34. **MVP voice does not talk back.** The user states exactly what they ate, so logging settles silently. Voice replies / conversational mode are a later opt-in layer.
35. **Protocol model = calories per kg of IDEAL body weight** (fat loss ≈ 24–29 cal/kg IBW; placement in the band set by the human intake — stress/training/age/meds), NOT Mifflin-St Jeor TDEE. Protein scales with bodyweight; water ≈ half bodyweight in oz; fiber ∝ calories; produce = servings/day. Engine must be **formula-pluggable** — Francesco's real decision-tree formulas arrive via Notion (likely under NDA) and drop in. Supersedes the Mifflin approach in the old PROTOCOL_LOGIC draft.
36. **Activity level is inferred, never asked.** Everyone over-rates themselves. The intake asks occupation + routine + obligations + steps + meds + stress; the engine derives activity. The deep intake also exists to make the user *feel seen* (a retention driver, not just accuracy).
37. **Monthly recalibration is decision-tree, not judgment** (later, design for it): recalibrate to adjusted IBW on weight change; no-progress-but-compliant → knock cal/kg down one point; "why no progress?" surfaces honest diagnostics (movement, logging accuracy).
38. **The conversational AI is a "guide," not a coach** (later, post-MVP): ChatGPT prompted to Serein/Francesco voice — guides, doesn't tell people what they want to hear. Uses the `guide` + `serein-voice` skills; draws on a bank of Francesco's situational moves.
39. **RESOLVED 2026-06-18 → NATIVE iOS / TestFlight.** Lorenzo's call. The voice-quality/trust thesis is best tested on the real Serein-backed native capture (already ported, 9/9 on sim), not a degraded browser-voice build. The FastAPI backend already serves it. Frontend build resumes on the native path: Phase D (voice-log UI), E1/E2, F-UI, G-UI, C5 (on-device transcription), then Phase I → TestFlight. Original open framing kept below for the record.
   **(was) OPEN: platform — web-app MVP vs native iOS / TestFlight.** The call leaned web app (vocal.com link beats App Store friction for the warm-list/referral GTM); but voice quality is the thesis and Serein (native, already ported C0–C3) is why voice is good. The FastAPI backend (A/B/C4) is frontend-agnostic and serves either. Decision deferred to Lorenzo + Francesco — see the master-plan amendment. Until decided, no frontend rebuild; backend work continues to serve both.
40. **UI reference (future builds):** the green/cream nutrition app Lorenzo shared (food-preference chips in onboarding, water as a first-class dashboard card, ingredient-detail with short descriptions) is a *layout/interaction* reference only — **palette stays black/gold** (Lorenzo: "I like what you have currently"). Logged in `docs/DESIGN.md`.

## 2026-06-18 — Navigation / information architecture (Cal-AI-style, Lorenzo screenshots)

41. **Tab bar = Home · [center Log] · Progress.** **Home** is the overview/dashboard (calories left, protein, produce/water/fiber, meals logged) — rename "Today"→"Home". A prominent **center Log button** (black circle) is the primary action. **Progress** is the right tab. Settings/profile is a small header affordance on Home, not a tab. **No Groups/social tab** (social is out of P0 scope). This restructures the earlier Today/Settings 2-tab shell.
42. **Logging is meal-type-first.** Tapping center Log opens a quick **meal-type picker (Breakfast / Lunch / Dinner / Snack)**; selecting one **auto-advances straight into voice capture** (centered mic → listening → transcribing → enhancing → parse → confirm), with the meal type pre-set so the result screen never re-asks. Matches the reference app's flow.
43. **Progress tab** (new screen): weight + goal, a logging **streak**, a **weight-trend chart** (90D/6M/1Y/ALL), daily-average-calories trend, and a strength-based encouragement line. Weight is self-reported (no HealthKit in P0, decision #17). This is where "Log Weight" lives. Maps to Phase E (dashboard/metrics) as a second screen, and feeds the weekly/mid-week nudging (Phase G).


## 2026-06-18 — Variant-axis threshold (realizes cofounder intent)

44. **Variant ("which product?") checks fire on a lower bar** than amount/state/fat-ratio: `VARIANT_THRESHOLD_KCAL=40` / `VARIANT_THRESHOLD_MACRO_G=4` vs the uniform 75/10. Rationale: choosing whole vs fat-free cheddar or regular vs light mayo is a single tap among 2-3 known options — cheap and certain — so it is worth asking at a smaller swing than a vague amount estimate. Without this, a single cheese slice (~69 kcal swing) sits just under the uniform bar and the engine would not ask cheese/mayo type — contradicting Francesco's explicit "cheese type and mayo type should pop as checks." With it, the canonical burger fires beef fat-ratio + cheddar type + mayo type + mayo amount (capped at 4). Revertible by setting the variant thresholds equal to the uniform ones; lives in parser/clarify.py.