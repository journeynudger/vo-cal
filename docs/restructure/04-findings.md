# Findings ledger (Phase 7.5)

Every defect noticed and deliberately not fixed in this pass, with its address, its risk, and
why it was out of scope. Date: 2026-09-23, branch `restructure/capture-first`.

| # | Finding | Address | Risk | Why not now |
|---|---|---|---|---|
| 1 | `POST /metrics/client` has no producer: the iOS app never posts, so `scripts/beta-metrics` and the ARCHITECTURE.md claim that "the beta-gate numbers come from here" describe an empty table. | `services/api/src/api/metrics_ingestion.py`, `scripts/beta-metrics`, `docs/ARCHITECTURE.md` | The six beta-gate numbers are unmeasurable today; a doc that lies. | A client telemetry producer is a feature with privacy review (MUST NOT #5), not a structural change. Question 5 in the questions ledger. |
| 2 | Resuming an unfinished capture whose worker upload already succeeded re-sends the audio: `uploadNow` always uploads and relies on the server's dedup, because the local outbox does not keep the server capture UUID that `/transcribe` needs. | `CaptureUploadWorker.uploadNow`, `LocalCaptureRecord` | Wasted bytes on resume (bounded by the 50 MB cap); correct result. | Fixing it well needs a `GET /captures/by-client-id/{id}` or the UUID persisted locally: an API addition, out of the freeze. |
| 3 | A capture recorded while browsing a past day is listed under the day it was recorded and resumes to that day, not the day the person had selected. | `CaptureOutcomeStore.unfinishedCaptures`, `VoiceLogViewModel.resume` | Rare; the meal lands a day off and the person has no way to move it. | Recording the target day would add a ledger write on every capture; a move-meal-day edit is the general fix and a product decision. |
| 4 | `Database.select_owned_via` uses a PostgREST inner embed (`meal_logs!inner(user_id)`) that the offline suite cannot exercise; only the fake is tested. | `services/api/src/api/db.py` | Proven live 2026-09-23: the deploy's authed smoke ran `POST /parse` against production (run 35935400967, then 35936025949), which derives the learned map through this query, and answered 200. | Closed. |
| 5 | The purge of tombstones past 30 days has no scheduler; it runs when an operator calls the admin endpoint. | `admin/router.py::purge_deleted_meals` | Tombstones accumulate (hidden, small rows) until someone runs it. | A scheduler is infrastructure (Fly cron or a GitHub schedule with an admin token) and a decision about where secrets live. Question 1. |
| 6 | Learned names apply after a single rename. A one-off contextual rename ("chicken" to "chicken thigh" once) becomes the default until reverted or forgotten. | `meals/learning.py` | A surprising rename on the next meal; visible and reversible (revert at confirm unteaches, Settings > Learned names has Forget). | Product decision between "learns at once" and "learns after two". Question 2. |
| 7 | `count_corrections` excludes `name_forget` rows, so a meal's `corrections_count` no longer counts a Settings Forget against it; the admin review chain still lists the row. | `meals/store.py` | None for people; an admin reading the count and the rows sees one more row than the count. | Intended; recorded so the next reader does not "fix" it. |
| 8 | `HorizontalPull` on the week strip was verified on the simulator build only; the tap-cancels-on-move behavior of the strip's buttons under a real finger is Serein's evidence, not Vo-Cal's. | `apps/ios/VoCal/Views/Today/TodayView.swift` | A pull could end in a day selection on some hardware. | Needs a device session (TestFlight build 27). |
| 9 | `CaptureOutcomeStore.scanLimit` is 500 newest captures: a day further back than 500 captures reads as having nothing unfinished. | `apps/ios/VoCal/Services/CaptureOutcomeStore.swift` | None at beta scale (months of captures). | A day-indexed outbox query would be the bound-free answer; not worth a schema change now. |
| 10 | `RuntimeMode.startsOnSettingsTab` and `showsWeekBudgetOnLaunch` were census candidates; both are read (`VoCalApp.swift:101`, `TodayView.swift:39`) as headless-verification hooks. | `apps/ios/VoCal/Services/RuntimeMode.swift` | None. | Not dead; recorded so the census is not repeated. |
| 11 | The Beacon organization on Fly runs a started 512 MB machine on an app whose status is "suspended", and two empty Vocal organizations carry paid plans. | Fly dashboard (outside this repo) | Money, not correctness. | Another organization's plan and apps are the owner's to change. Recommendation in the final report. |
| 12 | An `NWPathMonitor` "network back" trigger for the upload worker was skipped: its API takes a `DispatchQueue`, which TIDY-CONC-001 bans. The scene-active kick and the planner's wake cover the common case. | `CaptureUploadWorker` | A capture made offline uploads at the next scene activation or backoff wake, not the instant the network returns. | An exception path to a ratchet is a stop-and-ask item (rule 1). Question 3. |

## Second pass, 2026-09-23 (audit/deeper-cleanup, merged as 9dbd800)

| # | Finding | Address | Risk | Status |
|---|---|---|---|---|
| 13 | The offline API suite was not hermetic: with a .env beside the API, build_resolver wired the live Anthropic estimator and the live USDA client, so two meal tests spent 14 s and money per run and the corpus scores could have drifted with the network. | `services/api/tests/conftest.py`, `tests/test_hermetic.py` | Fixed: provider keys blanked for every non-live test, socket-opening httpx transports refused, both pinned by a test that failed before the fix. |
| 14 | Every day view, window read, water tally, check-in history and purge sweep read the user's whole table into Python (F6). | `services/api/src/api/db.py`, `meals/store.py`, `checkin/store.py` | Fixed: where/order_by/limit in the database layer, instants compared as instants in the fake. The live PostgREST path (`gte`/`lt`/`is_`/`order`/`limit`) is proven by the deploy smoke's new day read: `GET /meals/today` answered 200 against production in run 35936025949, on a day with a row just written. |
| 15 | 1,255 lines of Serein-port surface no caller reached: twenty outbox methods, five test hooks, telemetry and filename-semantics types, the share and shortcut surfaces, a dead view and font role. | `apps/ios/VoCal/Voice`, `Sources/VoCalCapture` | Deleted with the census (`scripts/swift-census`); the compiler caught the one wrong deletion before verification. |
| 16 | `RelayPlanner.plan(snapshot:...)` and the `OutboxSnapshot` it reads are reached only by `claimEligibleRelayJobs`; the planner tests pin them. Not dead, recorded so the census is not repeated. | `Sources/VoCalCapture/RelayPlanner.swift` | None. |
| 17 | `web/` (the marketing landing page, 56 KB, self-contained) is referenced by nothing in the repository and deployed from nowhere in it. Kept: it is the only copy. | `web/index.html` | None; a question for the owner (questions ledger 8). |
| 18 | The CI app job spends 183 s compiling cold and 340 s on the simulator step (boot, install, twelve real recordings). Nothing in it is waste to cut without shortening the recordings the scenarios need. | `.github/workflows/ci.yml` | None; the local run is 42 s warm. |
| 19 | The voice runtime gate could fail for a reason that is not the code: the simulator microphone grant was best-effort (5 s, failure swallowed), and when it silently missed on a macOS runner every recording scenario failed with `voice_microphone_permission_missing` (CI run 35936026705 on a scripts-only commit that had passed 12/12 minutes earlier). | `bin/ios-sim-voice-test` | A red gate that blames the app for the runner. | Fixed: the grant gets 30 s and three attempts, and a grant that still fails stops the run with a message naming the simulator, not the app. |

## Third pass, 2026-09-24 (the Rams audit and personal foods; docs/restructure/06-rams-audit.md)

| # | Finding | Address | Risk | Status |
|---|---|---|---|---|
| 20 | Personal foods are consulted by exact normalized name (with "my", "a serving of", "recipe" stripped) and aliases; a transcriber mishearing that the learned-names pass has not yet mapped misses the food and prices as before. | `services/api/src/api/foods/index.py` | The person renames once and the learned pass covers it from then on. | Accepted; recorded so the two mechanisms are not merged. |
| 21 | A serving whose weight the person did not state is carried as 100 g inside the identity; nothing a person sees shows it, and a stated weight for such a food prices by servings only. | `foods/index.py` UNWEIGHED_SERVING_GRAMS | An admin reading grams on such a meal sees 100 per serving. | Accepted and commented at the constant. |
| 22 | The mock path's recipe sheet sums on the phone (the simulator has no server); the live path never does. | `MockPersonalFoodsService` | None for people. | Accepted. |

## Fourth pass, 2026-09-24 (FatSecret and the UI verification loop; docs/UI_VERIFICATION.md)

| # | Finding | Address | Risk | Status |
|---|---|---|---|---|
| 23 | The first UI goldens were blank pages and the tests passed against them: `ImageRenderer` draws nothing inside a `ScrollView`. | `apps/ios/VoCalRenderTests/RenderHarness.swift` | A loop that cannot see the content certifies nothing. | Fixed: window hosting plus a blank guard that throws. |
| 24 | Byte-identical goldens still failed at random: the result screen's Liquid Glass close button moves a few hundred pixels between renders, and the library's colour compare had a 0.22 percent baseline from the renderer's extended colour range. | `RenderHarness.swift`, `SnapshotPolicy.swift` | Flaky goldens get loosened until they prove nothing. | Fixed: standard-range sRGB, tolerance set at the measured floor, five verify runs stable. |
| 25 | FatSecret refuses a freshly allow-listed IP from some edge nodes for over an hour (error 21), and the fixture recorder wrote a refusal body as a fixture, so every replay test failed for a reason unrelated to the code. | `nutrition/fatsecret_client.py`, `tests/food_source_eval.py` | Refusals read as "the platform lacks this food" and the comparison lies. | Fixed: three retries in the client, refusals counted apart in the eval, the recorder refuses to write a refusal. |
| 26 | The curated suffix head priced "chicken salad", "egg salad" and "potato salad" as a green salad; USDA's relevance gate accepted "Apple Crisp" for "crisp apple" by tokens alone. | `nutrition/resolver.py`, `fatsecret_client.py` | Silent mispricing of common foods. | Fixed: FatSecret before the head, a row must end in the word said last. |
| 27 | The CI runner's Xcode carries a different iOS than the pinned simulator, so goldens cannot compare there. | `SnapshotPolicy.swift`, `.github/workflows/ci.yml` | A golden compared on another runtime fails for the wrong reason or passes for one. | Accepted: goldens pin their runtime and skip out loud; CI runs the non-golden tests. |
| 28 | Production has no static egress IP; FatSecret's allow list is per IP. | Fly app `vo-cal` | A machine move changes the IP and every FatSecret lookup is refused until it is listed again. | Open: allocate a static egress IP before `FATSECRET_ENABLED=true`. |
| 29 | Eleven controls have hit areas under 44 pt (`bin/ios-ui-audit`, 2026-09-24): on Today the water card and its two number labels, the "Show pro tip" toggle, the selected day chip and the protein stat card; on the settings pages the Forget and Restore text buttons. Every theme font is a fixed point size (74 labels flagged for Dynamic Type across the five audited pages) and the muted ink on cream fails or nearly fails contrast on 54 labels. | `TodayView`, `WeekStrip`, `StatCard`, `NudgeCardView`; `VoCalTheme.Fonts` and `.Colors.muted` | Hard to tap for some hands; unreadable for some eyes. | Open, ratcheted: the audit fails on any count above its committed baseline; fonts and contrast are decisions 12 and 13 in `05-questions.md`. |

## Fifth pass, 2026-09-25 (the UX overhaul: names, one bar, text and photo, the tour, Health, Siri)

| # | Finding | Address | Risk | Status |
|---|---|---|---|---|
| 30 | Every logged meal read "Meal": the client sent the meal-type word as the meal's name on every confirm, and the server stored it as the person's. | `VoiceLogViewModel.confirm`, `meals/naming.py typed_name` | The one thing Lorenzo saw first. | Fixed: the client sends no name, the server reads a meal-type word from any build as no name, and every meal is named after its items. |
| 31 | One estimated item held an eight-item parse at 17.5 s: the grounded search, its JSON follow-up and the knowledge lane ran back to back with no clock. | `nutrition/estimator.py` | Logging felt broken at exactly the meals that needed the estimator. | Fixed: a race with a 6 s deadline and a 10 s cap (resolution p95 9.6 s to 6.3 s on the probe); Haiku for the parse call (p50 3.2 s to 1.4 s). |
| 32 | The week banked a thin day's shortfall as headroom and raised the days left. | `weekbudget/engine.py` | "Make up three days of calories". | Fixed: overages carry, shortfalls do not, untracked days are on plan. |
| 33 | Glass cards in a scrolling list drop frames (Serein's finding, kept): only the chrome is glass here (the bar, the profile circle, the result's pinned bar); cards stay flat. | `docs/DESIGN.md` | A janky scroll on the one screen people live on. | Accepted as a rule; `bin/ios-motion` measures it. |
| 34 | The photo path stores the object key in `captures.audio_path` and the admin chain mints signed URLs from the audio bucket, so an admin review of a photo capture shows no media. | `admin/store.py get_log_chain` | Admin only; the photo is stored and owner-scoped correctly. | Open: pick the bucket by `content_type` in the admin chain. |
| 35 | There is no offline outbox for photos: a photo taken without a connection is refused at once, not saved and retried (the upload worker is audio-only, off the capture path by construction). | `parser/photo.py`, `docs/CAPTURE_LIFECYCLE.md §9` | A photo lost to a dead spot, said out loud. | Accepted for this build; revisit with a photo outbox that shares the worker's door. |

| 36 | Production's `PARSER_PROVIDER` secret said openai while the pass moved `PARSER_MODEL` to claude-haiku-4-5: the OpenAI client was asked for a Claude model and every `POST /parse` answered 500 from the release until the smoke stage failed it (Deploy run 36053036184). | `parser/llm.py provider_for`, `parser/router.py get_parser_client` | Every voice log in production for the minutes between that release and the fix. | Fixed: the model id picks the provider and the provider setting only settles ids without a family, pinned by `tests/test_parser_provider.py`. The smoke stage did its job after the release; a boot-time refusal would have kept the old release serving, noted for the deploy lane. |
| 37 | The capture bar had no way out of the composer: no catcher under the bar, and the mark with nothing to send was disabled, so the keyboard held the page (Serein's catcher was cut in the port). | `VoCalApp.swift catchers`, `CaptureBar.sendButton` | The first thing a typed log meets. | Fixed: Serein's catchers under the bar, the mark is Done when empty, `CaptureFlowTests`. |
| 38 | A finger landing a few points above the plus opened the week's budget or the unfinished recording behind the bar (Serein's dead zone was cut in the port). | `CaptureBar.deadZoneStrip` | Read on the phone as "the plus opens the weekly log". | Fixed: a 36 pt dead zone above the row, the plus at the mic's 56 pt, pinned by the near-miss flow test. |
| 39 | The arming, listening and idle rungs drew through three switch branches, so the scaffold was rebuilt between Starting and Listening while a repeat-forever pulse had no clean stop: the mic jumped, for the second time (2026-07, build 30). | `VoiceLogView.captureRung` | The one animation every voice log shows. | Fixed: one branch, one identity, the ring animated in place, no pulse; the flow test samples the mic's centre through the start. |
| 40 | The calories and protein cards had two structures and two heights, centred against each other. | `TodayView.splitCard`, `CalorieBar` | The first two cards on the page. | Fixed: twins (title, 40 pt numeral, bar, line), `fixedSize` on the row. |
| 41 | The Action button card was a card inside a sheet. | `ActionButtonSetupCard`, the shell's inset | A container in a container, at the one moment the app asks for a favour. | Fixed: on the page above the bar, Serein's coach card on Vo-Cal frost. |
| 42 | Usuals could be forgotten but not renamed, while logged meals could. | `PATCH /meals/usuals/{id}/name`, `TodayView` rename target | Two ways to name one thing. | Fixed: one alert for both, 409 on a name another usual carries. |
| 43 | Every loop was green while three flows were broken: renders, metrics and the audit never drive the bar. | `bin/ios-flow-tests`, CI's iOS job | The gap the phone found. | Fixed: the flow loop, a minute, in CI. |
| 44 | A `.glassEffect` contributes no hit region on iOS 26.5: a glyph on glass is hittable only through what is drawn on it, and a rim under a point does not count, so the plus, the profile circle, Settings' close and the capture's big mic took no touch at their centres while accessibility reported and activated them (every hand check through the simulator tool passed). Serein's glass buttons carry `.contentShape(Circle())`; the port dropped it. Bisected with a launch-argument variant switch under `CaptureFlowTests`, nine launches, repeats stable: rim 1.5 pt takes the touch at any tint, 0.75 pt and none do not, interactive or not. | `LiquidGlass.swift` | "The plus opens the weekly log": the touch went through the plus to the week card. | Fixed: the glass modifier makes its shape the hit region for every surface; the big mic gets its disc; the finding lives at the modifier; the flow test taps by touch, never by activation. |
| 45 | The capture scaffold's reserved Stop slot was an empty `Group` until listening, so it had no frame, and the mic sat 28 pt lower while arming than while listening (the 2026-07 fix reserved a slot that did not exist). | `VoiceLogView.captureScaffold` | The mic jump Lorenzo saw, measured. | Fixed: a clear 56 pt view holds the slot; the flow test samples the mic's centre through the start and allows under a point. |
