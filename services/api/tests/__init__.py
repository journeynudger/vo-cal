"""Vo-Cal API test package.

A package (not a bare directory) so shared test infrastructure — the fixture
corpus loader (``tests.corpus``) and recorded-response helpers — import the same
way from the pytest suite and from ``scripts/parser-eval``.
"""
